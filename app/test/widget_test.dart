// Placeholder test. The app's widgets depend on Firebase.initializeApp()
// having run, so meaningful widget tests need Firebase test mocking, which
// is not set up yet for this project.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
